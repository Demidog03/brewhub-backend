info:
  name: register
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Test Dulat1",
        "email": "testdulat1@example.com",
        "password": "Dulat123456"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
