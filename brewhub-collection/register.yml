info:
  name: register
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Test User 1",
        "email": "testuser1@example.com",
        "password": "Qwerty123!"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
